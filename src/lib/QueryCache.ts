/**
 * QueryCache is an alias for QueryClient for backwards compatibility
 */
export {
  QueryClient as QueryCache,
  type QueryClientOptions as QueryCacheOptions,
  type AddPromiseOptions,
  type PromiseEntry,
} from "./QueryClient";

